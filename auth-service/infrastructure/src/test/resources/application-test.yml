spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: false
    defer-datasource-initialization: true

  sql:
    init:
      mode: never

security:
  jwt:
    secret: mySecretKeyabcd123456789mySecretKey123456789zxyw
    expiration-ms: 3600000

logging:
  level:
    org.springframework: WARN
    org.hibernate.SQL: OFF

sso:
  authorize-url: https://fake-sso-provider.com/oauth/authorize
  client-id: auth-service-client
  redirect-uri: http://localhost:4200/sso/callback
  valid-code: valid-code
  mock-user: sso-user@mail.com